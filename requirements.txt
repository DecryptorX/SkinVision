Flask==2.3.3
Flask-CORS==4.0.0

# Use CPU-only PyTorch builds (compatible with Railway/Linux).
# If you need GPU, update to the appropriate CUDA build and runtime.
-f https://download.pytorch.org/whl/cpu/torch_stable.html
torch==2.0.1+cpu
torchvision==0.15.2+cpu

Pillow==10.0.0
numpy==1.24.3